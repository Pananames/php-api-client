{
  "type": "array",
  "properties": {
    "data": {
      "type": "array",
      "items": {
        "type": { "$ref": "#/definitions/tldInfo" }
      }
    }
  },
  "required": ["data"],
  "definitions": {
    "tldInfo": {
      "properties": {
        "tld": { "type": "string" },
        "idn": { "type": "boolean" },
        "dnssec": { "type": "boolean" },
        "prices": { "$ref": "#/definitions/price" },
        "promo_prices": { "$ref": "#/definitions/price" },
        "promo_untill": {
          "type": "string",
          "format" : "date-time"
        },
        "promo_two_years_prices": { "$ref": "#/definitions/price" },
        "promo_two_years_untill": {
          "type": "string",
          "format" : "date-time"
        },
        "promo_multi_years_prices": {
          "type": "array",
          "properties": {
            "promo_multi_years_prices": { "$ref": "#/definitions/price" },
            "promo_multi_years_untill": {
              "type": "string",
              "format" : "date-time"
            }
          }
        }
      },
      "required": ["tld", "prices", "idn", "dnssec"]
    },
    "price": {
      "type": "array",
      "properties": {
        "currency": { "enum": ["usd", "eur"] },
        "register": { "type": "number" },
        "renew": { "type": "number" },
        "transfer": { "type": "number" },
        "redeem": { "type": "number" }
      },
      "required": ["currency", "register", "renew", "transfer", "redeem"]
    }
  }
}